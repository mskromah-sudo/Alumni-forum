import React, { useState, useEffect } from "react";

export default function EventBoard({ userRole }) {
  // List events, RSVP button, propose event (for alumni), event gallery
  return (
    <div>
      {/* Event list, RSVP, create/propose event, gallery for photos/videos */}
    </div>
  );
}